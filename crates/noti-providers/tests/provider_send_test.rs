//! Comprehensive wiremock-based tests for provider send() methods.
//! Tests both success and failure paths for representative providers.
//!
//! This file is the root module that re-exports all category-specific test files.
//! Each category file corresponds to the provider categories in providers.rs.

mod provider_send;
